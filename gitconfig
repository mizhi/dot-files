[push]
	default = simple

[user]
	email = mizhi@cognius.com
	name = Mitchell Peabody

[alias]
	dfw = "diff --ignore-space-change"
    cc = "!f() { echo \"Number of commits from $1 to HEAD: \"; git log $1..HEAD --pretty=oneline | wc -l; }; f"

[filter "media"]
	clean = git-media-clean %f
	smudge = git-media-smudge %f
[core]
	editor = vim
